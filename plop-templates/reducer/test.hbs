import {{name}} from "../{{name}}";

describe("Reducers", () => {
  describe("{{name}}", () => {
    it("should initialize the state", () => {
      const newState = {{name}}(null, { type: "???" });
      expect(newState).toBe(/* TODO: Set to initial state */);
    });
  });
});
